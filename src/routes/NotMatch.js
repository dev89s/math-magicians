function NotMatch() {
  return (
    <h2
      style={{
        padding: '10px 30px',
        maxWidth: '768px',
        margin: 'auto',
      }}
    >
      There is no match for the entered address
    </h2>
  );
}

export default NotMatch;
